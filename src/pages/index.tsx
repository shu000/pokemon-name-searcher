import React, { FC } from 'react';
import { PageTemplate } from '../containers/pageTemplate';

const IndexPage: FC = () => (
  <PageTemplate title="">
    <h1>now building</h1>
  </PageTemplate>
);

export default IndexPage;
